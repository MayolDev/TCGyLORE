## 2025-02-23 - Custom File Input Accessibility
**Learning:** Hidden file inputs (`opacity-0`) must have a parent container with `:focus-within` styles (e.g., `ring-2`) to ensure keyboard users can perceive focus location.
**Action:** Always wrap custom file inputs in a container with `focus-within` styles.
